{
  "name": "uzmedatlas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix back && npm run setup --prefix back && npm install --prefix front",
    "dev": "concurrently -n back,front -c blue,green \"npm run dev --prefix back\" \"npm run dev --prefix front\"",
    "test": "npm test --prefix back",
    "build": "npm run build --prefix front"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
